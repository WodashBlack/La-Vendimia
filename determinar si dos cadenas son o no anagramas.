//Metodo para determinar que dos cadenas son anagramas    
    
    public static boolean esAnagrama(String cad1, String cad2) {

        char[] cadena1 = s1.toCharArray();
        char[] cadena2 = s2.toCharArray();

        Arrays.sort(array1);
        Arrays.sort(array2);
        return new String(array1).equals(new String(array2));
    }
